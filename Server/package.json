{
  "name": "server",
  "version": "1.0.0",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/index.js --ignore frontend",
    "api": "echo 'Local API listening on PORT 5000' & json-server --watch api/comunas.json -p 5000 -q",
    "start": "concurrently \"npm run dev\" \"npm run api\""
  },
  "dependencies": {
    "axios": "^1.6.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "json-server": "^0.17.4",
    "morgan": "^1.10.0",
    "pg": "^8.11.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^2.0.22",
    "npm-run-all": "^4.1.5"
  }
}
